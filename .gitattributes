# 仓库在 Windows 上开发、在 RK3588 的 Linux 上运行。脚本一旦带 CRLF，
# bash 会把 \r 当成命令的一部分而报错，所以强制这些文件用 LF。
* text=auto eol=lf

*.sh   text eol=lf
*.py   text eol=lf
*.service text eol=lf
*.bat  text eol=crlf
*.ps1  text eol=crlf

*.png  binary
*.ico  binary

# 自解压安装包是「shell 壳 + 附加的 gzip 数据」。上面那条 text=auto 靠启发式
# 判断二进制，对这种前半段全是可打印字符的文件并不可靠 —— 一旦被当成文本做
# 换行规范化，数据段就废了，而且要等装到板子上才会发现。所以写死。
*.run    binary
*.tar.gz binary
